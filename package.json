{
  "dependencies": {
    "firebase-tools": "^7.7.0"
  },
  "scripts": {
    "build": "cd flt && flutter build web",
    "clean": "cd flt && flutter clean",
    "firebase": "firebase",
    "deploy": "yarn clean && yarn build && firebase deploy --except functions",
    "serve": "yarn build && firebase emulators:start"
  }
}
